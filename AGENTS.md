# 仓库指南

## 项目结构与模块组织

DogPaddle 是一个 Rust 2024 工作区。根目录 `README.md` 只介绍产品定位、已有能力和当前边界。`crates/change/` 定义共享 Arrow Schema、`Change`、Schema 校验、Schema 绑定的顶层投影，以及每个 Change 独立、自描述、恰好一个 RecordBatch 的完整 Arrow IPC Stream 编码与选择性读取；它的库代码与正常依赖不依赖 Flow、Operation 或 Store。`crates/store/` 实现 MDBX 事务存储、编解码器和类型化集合；每个持久化集合以最后一个 `Small` 或 `Large` 泛型参数显式声明物理规模，Store 直接创建或打开具体集合实例，裸句柄与 placement 不对外开放，且不依赖 Arrow。Store 同时提供唯一写事务启动能力、可共享的只读 snapshot 启动能力，以及 collection handle 和 transaction access 两层读写权限控制。`crates/operation/` 承载具体 Operation 的纯 Definition、稳定编码、精确 logical Arrow Schema 绑定、持久化 Data 声明和执行语义，运行 trait 与具体算子统一放在 `operation/`，并在其下按 `source/`、`transform/`、`sink/` 分类，当前包含 SequenceSource、Count、Project、Filter、Extend 与 Discard；共享记录模型不属于 Operation。`crates/flow/` 提供公共 `FlowFactory`、拓扑与 Schema 校验、持久化 `build/open`，并拥有内部 Station；`Flow` 只表示已经构建或重新打开的运行态对象，并持有 Store 分离的读写事务启动能力。dogpaddle-flow 内部按生命周期拆分：`build/` 统一拥有 `FlowFactory` 的声明与 build/open 路径、`StationRef`、Flow/Station Definition、纯校验和稳定编码，`build/schema.rs` 在资源创建或打开前按拓扑完成全图 Schema 传播与 Operation binding，构建时随后创建全部资源，`build/open.rs` 完成两阶段重新打开；`flow/mod.rs` 只声明模块，`flow/runtime.rs` 保存运行态 `Flow`、全部 Station、派生的确定性拓扑 schedule、生命周期状态及读写事务能力，`flow/advance.rs` 保存公共单轮调度 outcome 与 `Flow::advance`；`station/mod.rs` 也只声明模块，`station/runtime.rs` 保存 Station 装配与 `process` 边界，`station/input.rs` 保存统一 `Output`、`Inbox`、owned `Claim`、durable active/cursor、per-edge acknowledgement capability 与内联 reclaim，`station/protocol.rs` 保存 outcome/error；私有 `assembly.rs` 只承接 build/open 共用的 source ID 解析、分层拓扑 schedule 派生与最终 Station 装配，不形成公共抽象。Station 不长期持有事务启动能力，不接收 Store，不创建或打开资源，也不知道物理 placement 或稳定资源名。拓扑是 Flow Definition 的连接关系，不再拥有独立构建类型；声明入口统一为 `FlowFactory::new(path)`。`integration-tests/change-store/` 是不可发布的下游测试包，只通过公共 API 验证完整 Change Stream 与 `AppendLog<Vec<u8>>` 的接缝；产品 crate 不得依赖它。每个产品 crate 的语义、用法和验证方式写入自身 `README.md`，并作为 Rustdoc 首页；不可发布的测试 package 只需维护自身测试说明。全工作区测试所有权、数据规格和性能口径统一写在根目录 `TESTING.md`。

## 构建、测试与开发命令

- `cargo build --workspace`：使用工作区锁定的依赖构建四个产品 crate、不可发布的集成测试包与内部测试工具。
- `cargo test --workspace`：运行单元测试、集成测试和文档测试。
- `cargo test -p dogpaddle-change-store-integration`：只运行 Change 与 AppendLog 的外部组合测试。
- `cargo test -p dogpaddle-store --test correctness transaction::`：运行指定公共测试区域；所有 crate 的公共测试 target 都统一命名为 `correctness`。
- `cargo fmt --all -- --check`：检查格式，不修改文件。
- `cargo clippy --workspace --all-targets -- -D warnings`：执行已配置的 `all` 和 `pedantic` Clippy 规则。若命令不可用，请先安装 Clippy rustup 组件。
- `cargo xtask check`：运行格式、debug/release correctness、Clippy 与 Rustdoc 的统一工作区 gate。
- `cargo xtask bench-plan-check`：不创建 fixture 或计时，验证全部 benchmark 的 smoke/reference 纯 Plan 与相邻黄金指纹一致。
- `cargo xtask bench-smoke`：使用仓库固定的缩小参数实际执行全部 10 个 release benchmark target。
- `cargo bench -p dogpaddle-store --bench cell`、`--bench ordered_map`、`--bench append_log`、`--bench append_log_endurance`：分别运行 Cell、OrderedMap、通用 AppendLog 和 AppendLog 长稳 release 基准测试；Change 使用 `change_core`/`change_codec`，Operation 使用 `operation_core`，Flow 使用冷路径 `flow_lifecycle` 和单轮调度 `flow_runtime`，Change + Store 只保留接缝性能证据 `change_append_log`。工作负载与口径见根目录 `TESTING.md` 及各自的测试或性能说明。

请使用根目录 `Cargo.toml` 指定的 Rust 1.96 或更高版本。

## 编码风格与命名约定

遵循标准 `rustfmt` 输出，使用四空格缩进。模块、函数、变量和测试使用 `snake_case`；类型和 trait 使用 `UpperCamelCase`；常量使用 `SCREAMING_SNAKE_CASE`。工作区禁止 unsafe 代码。公共 API 必须提供文档；可失败的方法应包含 `# Errors` 小节。不要为旧 API 保留兼容层。

保持事务边界明确：需要显式作用域时，在同一个 `{ ... }` 中完成 `begin()`、所有访问和 `commit()`，不要为访问对象另设内层块。维持单向职责分离：Operation 不依赖 Flow，Station 只作为 Flow 内部运行单元存在，Store 不依赖其他引擎层。运行期 `Transactions` 不可克隆且唯一归 Flow 长期持有；Flow 在取得所有权时以 consuming `split` 显式获得同环境、不可克隆但可共享的 `ReadTransactions`，随后长期持有二者。

Station 不能保存任何事务启动能力：输入准备只在调用期间借用 `ReadTransactions` 与必要的 `&mut Transactions`。`Inbox` 独占 Station state、全部 input ports 和 owned `Claim`；没有 Claim 时，先在真正的 RO transaction 中从 durable active input 循环选择一个日志 entry；若选中 port 不同，必须在调用 Operation 前用独立短写事务把 active input durable-pin 到该 port，cursor 保持不变。已有的 `active input + cursor` 是唯一 durable input claim，Station 不得再持久化另一套 current-input 状态；owned `Claim` 只保存 port、offset 和解码后的完整 Change，是该 durable identity 的可丢弃内存副本。已有 Claim 时必须在开始事务前直接返回，输入准备不推进 cursor、不拆分或处理 Change、不调用 Operation，也不在每个 turn 前重复读取 Store 校验 Claim。`process` 只在调用期间接收 `&mut Transactions`，由它开始并提交写事务；consuming `split` 不能通过该借用调用，因此 `process` 无法导出并留存只读事务启动能力。

Operation 每个 turn 只接收 `Option<OperationInput>` 和不能提交的 `TransactionAccess`，并用自己持有的具体 `Cell` 或 `OrderedMap` 创建事务级 Access；它不能接收、开始、提交或保存 Transaction，也不能看到 AppendLog offset。Source 与其他 Operation 使用同一个 trait 和 Station 执行路径，只以 `turn(None, ...)` 表达零输入。返回协议只有 `Action::Idle`、`Action::Commit(Option<Change>)` 和 `Action::Complete(Option<Change>)`：`Idle` 必须回滚该 turn 的全部写入；`Commit` 提交 Operation 状态和可选 output，但保留已经提供的输入，零输入 Source 的成功 turn 也使用 `Commit`；`Complete` 只用于有输入的 turn，并声明当前完整 Change 已处理。input-free Operation 返回 `Complete` 属于协议错误。跨 turn continuation 必须存入 Operation 自己通过 Definition 声明的 Store state，不得存进 Station。由于 `Idle`、错误、output 容量拒绝或外层 commit 失败都可能重放 turn，Operation 不得在 `turn` 内执行无法随 Store transaction 回滚的可观察副作用；外部 Sink 必须使用另行定义的幂等提交协议。

Flow Definition 的 magic、版本、校验算法、Operation tag、每个 Station 的 output capacity，`flow/definition`、`station/{index:08x}/...` 资源名，以及 Station input cursor 的 key/value 编码都是开发期 v1 的持久化布局边界；修改时必须同步更新黄金字节或布局测试，但不提供旧格式迁移或兼容层。每个具有 output 的 Station 必须在 build 时显式获得一个持久化的非零 retained-byte capacity，Sink 不得配置；每个 Station 还必须获得一个显式为 `Small` 的 state map。每个 input cursor 必须在发布 Definition 的同一事务中显式初始化为 big-endian `u64` offset `0`，未来运行状态只能使用已声明资源。

`OperationDefinition` 是 operation crate 内的 sealed trait；每个具体 Definition 必须手动声明完整的 `OperationKind::Source`、`Transform(NonZeroU32)` 或 `Sink(NonZeroU32)`，role 与非零 input arity 不能从拓扑位置反向推断。Station 读取所包裹 Definition 的 kind 并向 Flow 提供 source、sink、输入数量和 output 属性；Flow 只校验 Station，不能枚举具体算子。当前一个 Station 包裹一个 Operation，未来组合多个 Operation 时应只扩展 Station 内部的属性归纳，不改变 Flow 的拓扑接口。具体 Definition 同时返回稳定的“逻辑名称 → 类型化 Store data class”声明，并把有序、精确的 input logical `SchemaRef` 纯绑定为一次性的 `OperationBinding`；Source 接收空 inputs，Source/Transform binding 必须给出一个完整 output Schema，Sink binding 必须没有 output。trait object 上不可覆盖的统一 `bind` 入口校验 input arity、全部 input/output DogPaddle Schema 与 kind/output 一致性，private sealed `bind_schemas` 只表达算子规则和 Schema 相关编译结果，外部调用方不能绕过公共校验。binding 不能访问 Store、外部 registry、时间或随机性；相同持久化 tag、payload 与有序 input Schemas 必须维持相同绑定语义。

`FlowFactory::build` 必须先稳定编码再解码 canonical Definition，并只从解码结果解析拓扑、按 schedule 传播 Schema 和绑定全部 Operation；全部 binding 与 data 声明成功后才能创建 Store。`open` 读取持久化 Definition 后也必须在打开具体运行资源前完成同一绑定。derived edge Schema 不单独持久化为 Cell、fingerprint 或 registry；Operation tag、payload、有序 input Schema 到 binding 的映射及任何 Schema 相关状态 codec 都属于 reopen ABI。`FlowFactory` 的 build/open 通路随后负责完整资源名并通用 create/open 具体 data 实例，再消费对应 `OperationBinding` 装配 Operation。`OperationBinding::materialize` 是 binding → runtime Operation 的单向边界：必须按名称和精确类型取出实例，不能依赖声明顺序；runtime Operation 只保存执行参数与已装配 collection，不得保存或暴露 Definition/binding，具体 Definition、binding closure 与运行 Operation 都不能接收 Store。运行态 `Flow` 不接收 Store，也不负责定义、创建或打开资源；dogpaddle-flow 的 build/open 通路不能枚举具体算子。

assembly 必须先从 target state 派生全部 consumer cursor capability，再把每个 producer 的 output log、capacity、精确 logical Schema 与完整 consumer frontier 唯一 move 进一个 `Arc<Output>`；producer Station 和所有 `InputPort` 只能共享该 Arc，不得 clone 或另存独立 log handle。Store 的 `Small`/`Large`、collection 类型和 codec 都是持久化 schema；Store 只验证 Size，Operation tag 对应的代码 schema 负责 collection 与 codec 一致性。Flow 的每个起点必须是 Source Station，每个终点必须是 Sink Station；允许多个起点、多个终点和多个合法 DAG 分量，Sink 没有 output，任何 Source 或 Transform output 都必须至少有一个直接 consumer。

`source`、`transform`、`sink` 分类模块必须能容纳任意多个算子，不得拥有或重导出分类级的单一 tag 或 decoder；稳定 tag 和 decoder 永远属于具体算子模块，公共 decoder 表按具体模块路径逐项注册。新增内建 Operation 必须注册唯一稳定 decoder，并覆盖 tag 唯一性、黄金字节、资源布局和 reopen。Project 是首个会拒绝合法 input Schema 的内建 Operation，其公共证据必须持续覆盖 Flow 级 `FlowError::Schema`、准确 Station ID、build 失败无目录副作用、decoded tag/payload binding golden 以及 build/open/reopen；以后每个新增的 Schema-sensitive Operation 必须补齐同等强度的自身规则证据，不得用 test-only Operation 代替真实产品语义。

Filter 与 Extend 的公共入口直接接收 DataFusion `Expr`；fallible `try_new` 使用 `datafusion-proto` 编码表达式，Definition 直接持久化这份 protobuf，并在 decode/open 时用同一 DataFusion 版本还原。DogPaddle 不再维护另一套表达式 AST、节点白名单、operator/type/nullability 规则或递归深度限制。Schema bind 必须通过 DataFusion `create_physical_expr` 建立 exact-input-Schema-bound `PhysicalExpr`，表达式类型、nullability、cast 与运行期 `evaluate` 语义均以 DataFusion 为唯一实现；该 API 假定 logical coercion 已完成，而 DogPaddle 不运行 logical/SQL planner，因此不额外插入隐式 cast，调用方需要显式 `cast`。DogPaddle 只负责 Definition/Flow 边界、完整 Schema guard 及 Change 语义。

DataFusion Expr protobuf 是 Expression payload 的版本绑定格式，不承诺跨 DataFusion 版本兼容。工作区必须精确 pin 相互匹配的 DataFusion、`datafusion-proto` 与 Arrow 版本；升级时必须审查 proto roundtrip、physical planning 和执行语义。若新版本不能读取或保持旧 payload 语义，必须 bump 外层 Operation Definition tag/version，并要求重建 Flow，不在同一版本内猜测或迁移旧表达式。Filter 的 tag 是 5，output Schema 精确等于 input，只保留 non-null true；全删返回 `Complete(None)`，部分筛选必须用同一 predicate 保持 records/diffs 对齐。Extend 的 tag 是 6，每个实例只追加一个由 `name + Expr` 唯一推导类型和 nullability 的字段，保留 input FieldRef 与 Schema metadata，不接受调用者重复声明 Field/type。二者都不声明 Operation data；公共证据必须覆盖 proto golden/roundtrip、build 静态拒绝无目录副作用、decoded Definition 的 bind/materialize/turn 语义、open 重新 binding、成功 build/open/reopen、Filter 空/全量/部分选择与携带混合 diff 的重批、Extend Schema metadata/nullability 和 Array/diff 共享。

公开的 `Flow::advance` 必须真实按派生的确定性拓扑 schedule 为每个 Station 至多提供一个 turn。input IPC 解码后、安装 Claim 或 durable-pin 前必须与共享 `Output` 的精确 logical Schema 匹配；不匹配不能产生持久化写入。Operation 返回 output 时必须在编码和 capacity 判定前与同一绑定 Schema 匹配；不匹配是协议错误并回滚整个 turn。`Idle` 丢弃本次写事务并保留 Claim；`Commit` 在同一写事务中提交 Operation continuation 和可选 output，但不得推进 cursor、轮转 active input、回收上游 entry 或清空 Claim；`Complete` 才在同一事务中提交 Operation 状态、可选 output、Claim 所属 input 的 cursor 推进、active input 轮转，以及必要的上游物理回收，并且只有 commit 成功后才清空 Claim。Complete 事务必须验证 Claim 的 port/offset 仍与 durable active input/cursor 相同。每个 producer output 在事务边界必须维持 `head == min(all consumer edge cursors)`；单个 Complete 只把一条 edge cursor 推进一个 offset，因此新最小值只能保持 head 或成为 `head + 1`，后者在同一事务中至多截断一个 entry。不得保留独立回收 phase、回收 debt 或每个 turn 前的 Claim 重校验。Operation 即使 output 已达容量也仍可运行；无 output 的 Commit 可以提交，实际 output 则由 AppendLog 的 capacity-aware append 判定。容量拒绝不是错误，必须回滚本次 Operation 事务并保留 durable identity 和 owned Claim，Source position、Commit continuation、Complete cursor/active/reclaim 都不得提前前进。只要尚未 `Complete`，下一 turn（包括 reopen 后）必须提供同一 `(port, offset, bytes)` 标识的完整 Change。公共结果按 `Progressed > Backpressured > Idle` 聚合，其中 durable pin 和 Complete 内联物理回收也属于 Progressed；背压不得跳过后续 schedule。当前仍不公开 `Flow::start`。

AppendLog 的稳定 metadata 保存 big-endian `head`、`tail` 和 `retained_bytes`。`retained_bytes` 只计算 `[head, tail)` 中每条 entry 的 8-byte offset key 与完整 encoded value，不包含 metadata、MDBX page、MVCC 或文件开销；所有 append 路径和实际 bounded truncate 必须在同一事务中精确维护，Flow 不得复制这套物理计费。非空日志只有在追加后不超过 capacity 时接受 entry；物理空日志按 `head == tail` 判断，并允许一条 oversize entry，因而该机制是 per-output soft high watermark，不是磁盘或内存硬配额。

Change crate 的 `Change` 是无事件时间、有稳定事件顺序、允许重复、未 consolidation 的 Arrow 批量差分，也是有序变化流的一个非空连续物理片段；行位置属于语义，`Change`、其 codec 和运行层不得排序或隐式抵消事件。`Change` 不携带应用前状态，因而不验证撤回是否存在；事件生产者必须遵守有效流契约，维护或物化相应关系的组件负责按记录等价关系保证任意记录的“应用前权重 + 已处理前缀累计 diff”非负，并在验证到负权重前缀时报错、回滚。AppendLog offset 只定位当前持久化分批中的完整 Change，Change 行位置只表达该片段内部的事件顺序，二者都不是稳定 event ID；Station input cursor 只持久化 offset，不保存 Change 片段内进度，Operation 自己声明的持久化状态拥有该 continuation。物理批次可以稳定合并或切分，但变换前后展平的输入事件序列必须逐项不变。Operation 的展平 output 事件序列和最终业务状态必须同时对稳定重批及同一 Change 的重复 `Commit` turn 切分保持不变，除非以后由独立的窗口、barrier 或 flush 信号明确引入语义边界；该比较域中的每种物理分批都必须能由对应的声明 Arrow input/output 类型表示。每个持久化 Change 必须编码成一个标准、完整、自描述的 Arrow IPC Stream。Stream 内嵌 physical Schema，第零字段固定为非 null Int64 `$dogpaddle.diff`，后续字段构成 logical record Schema；Stream 必须恰好包含一个非空 RecordBatch，并以 canonical EOS 完整结束。不得增加 DogPaddle 自定义 envelope、独立 Schema resource、Schema fingerprint 或 segment。运行层以 `AppendLog<Vec<u8>>` 保存编码结果，每个日志 entry 恰好保存一个完整 Stream，解码不得依赖 entry 外部 Schema。`ChangeProjection` 必须绑定精确 logical Schema，顶层字段索引严格递增，只能删列；diff 始终隐式保留，空投影合法，List/Struct 只按完整子树选择。内存投影必须共享原 Arrow buffer；选择性 IPC 解码必须先校验内嵌 Schema、完整 framing 和全部无需读取 payload 即可判断的 batch metadata，只复制并解码 diff 与所选字段，返回不借用 entry 或事务的普通 owned Change。未选字段的 UTF-8、List offsets 等值级约束不验证，需要完整审计时使用全量解码。投影是读取能力，不得改变写入字节、entry 大小或持久化格式。`$dogpaddle.` 字段名和 `dogpaddle.` Schema/Field metadata key 是保留命名空间。Arrow IPC metadata version、writer options、Schema marker、physical diff 布局、行序和允许的 Arrow 类型都是持久化兼容性边界；修改时必须更新完整 Stream 黄金字节、标准 Arrow reader 互操作、顺序保持、零/多 RecordBatch 拒绝、截断、尾随字节和 reopen 测试。Change crate 不实现 Store collection，Store 不依赖 Arrow。

## 测试规范

测试名称应清楚描述行为，例如 `finish_rejects_a_multi_station_cycle`。每个源码模块目录只维护一个 `tests.rs`，其中统一覆盖该目录内模块及子模块的私有实现；不要为单个源码文件再创建同名测试子目录。每个 crate 的公共行为和持久性测试只能有一个显式 Cargo target：`tests/correctness.rs` 作为入口、`tests/correctness/*.rs` 按领域拆分；manifest 必须关闭自动 test/bench 发现，产品 library 设置 `[lib] bench = false`，并逐项声明 target，防止重新碎片化。能经公共 API 证明的行为不得留在白盒测试中。

正常产品依赖的跨 crate 契约由组合根拥有：Operation + Store 归 Operation，Flow + Operation + Store 归 Flow。只有没有产品组合根的 sibling seam 才建立 `publish = false` 的 `integration-tests/<seam>` package；当前 `crates/change/tests/` 不得依赖 Store，`crates/store/tests/` 不得依赖 Change，必须同时依赖二者的行为、性能或长稳验证统一放在 `integration-tests/change-store/`。使用 `tempfile` 创建隔离的临时存储。普通测试不得使用 wall-clock 断言；benchmark 的 fixture、seed、预热和结果校验必须位于计时外，输出原始样本及 rustc/CPU/profile/git 信息，持久化 reference 运行还必须使用显式固定文件系统。目前没有硬性覆盖率指标；持久化变更必须覆盖成功构建、纯校验（包括 Schema binding）失败无文件副作用、不完整构建、资源布局、稳定编码和重新打开；运行期 Schema guard 必须证明 output 违例整 turn 回滚，以及合法 IPC 中的错误 input Schema 不安装 Claim、不 pin、不推进 cursor。完整规范见根目录 `TESTING.md`。

## 提交与 Pull Request 规范

遵循仓库已有的 Conventional Commits 格式，例如 `feat(flow): ...`、`perf(store): ...` 和 `refactor(store): ...`。标题应简洁、使用祈使语气，并限定到具体 crate。Pull Request 应说明行为及持久性影响、关联相关 issue、列出已运行的检查命令；涉及存储性能时，还应提供基准对比。产品定位或能力边界变化应更新根 README；crate 接口、语义或验证方式变化应更新对应 crate README 和 API 文档。不要提交 `target/` 或本地数据库文件。
